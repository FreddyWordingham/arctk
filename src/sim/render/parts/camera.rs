//! Camera structure.

// use crate::sim::render::Tracer;

/// Tracer emission structure.
pub struct Camera {}

impl Camera {
    /// Construct a new instance.
    #[inline]
    #[must_use]
    pub fn new() -> Self {
        Self {}
    }
}
