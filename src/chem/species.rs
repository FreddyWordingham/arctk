//! Chemical species structure.

/// Chemical species.
#[derive(Debug, Clone)]
pub struct Species {}

impl Species {
    /// Construct a new instance.
    #[inline]
    #[must_use]
    pub fn new() -> Self {
        Self {}
    }
}
